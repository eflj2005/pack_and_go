import 'package:flutter/material.dart';

class ColaborateScreen extends StatelessWidget {
  const ColaborateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Colaboradores',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
